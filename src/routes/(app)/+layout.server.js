// @ts-nocheck
// @ts-ignore

// import { authStore } from '$lib/stores';
// import { redirect } from '@sveltejs/kit';

export function load() {
	// console.log('from common app layout.js', cookies.get('userIsLoggedIn'));
	// cookies.set('userIsLoggedIn', true);
	// console.log('finished setting new cookie in layout: ', cookies.get('userIsLoggedIn'));
	// let currentAuth;
	// authStore.subscribe((newAuth) => {
	// 	currentAuth = newAuth;
	// });
	// console.log('currentAuth', currentAuth);
	// if (!currentAuth.currentUser) {
	// 	throw redirect(301, '/login');
	// }
	// return { cookies };
}
